# Local build outputs (rebuild on each machine)
/4dmap
/server/qserver

# Server runtime logs (local only)
/server/logs/
/server/server_qoe.log

# logs_exp: ignore new loose files at top level; allow versioned experiment bundles
/logs_exp/*
!/logs_exp/vm/
!/logs_exp/vm_run_*/
!/logs_exp/vm_runs/

# Local-only planning notes and editor metadata (not pushed to remote)
local-online-projection-weights-plan.md
AGENTS.md


