# Local build outputs (rebuild on each machine)
/4dmap
/server/qserver
qserver

# Server runtime logs (local only)
/server/logs/
/server/server_qoe.log

# Experiment logs: entire tree (Mininet/vm runs, sessions, loose *.log)
# Previously some paths were force-tracked with !rules — that caused accidental commits.
/logs_exp/

# One-shot helpers / VM state
.last_run_id
