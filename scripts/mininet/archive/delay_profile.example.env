# Path B on h1 (10.0.2.x / h1-eth1). Path A is h1-eth0.
IFACE=h1-eth1

# Steps: absolute seconds from when tc_delay_steps.sh starts, delay in ms (egress on IFACE).
# Sort by time ascending. Only one mode per run: use --dynamic-delay-profile (not loss).
0 20
25 80
50 150
